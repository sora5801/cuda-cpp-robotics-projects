# Push note — 2026-07-10-09: flagship 21.04 ssm

> Push-note per CLAUDE.md §7.1 — written **before** the push and included **in** it, so the
> repository always explains its own latest state.

## Summary

Flagship 21.04 — **speed-and-separation monitoring** — opens batch 1f. The full SSM chain runs on
synthetic top-down depth streams of a SCARA-style cell with an anonymous capsule "human": human
extraction with robot self-filtering, per-point minimum-distance fields, an ISO/TS 15066-*style*
protective-separation comparison, and a NORMAL→REDUCED→PROTECTIVE_STOP state machine whose
escalate-instantly/de-escalate-slowly hysteresis is argued from the false-stop/missed-stop
asymmetry. The gates are the strongest safety-flavored set in the repo: all four state
transitions land at **exactly** the closed-form predicted frames, with **zero false stops and
zero missed stops** over 240 frames. Per §8 this project says what it is everywhere it can: the
demo's own stable output carries `NOTICE: didactic implementation — NOT a certified safety
function`, and PRACTICE §4 explains what real certification adds (certified sensors, redundancy,
validated stopping models). Person-perception is collaborative-safety only — the human is an
anonymous capsule by design.

## What changed

- **[projects/21-hri-teleoperation/21.04-speed-and-separation-monitoring/](../projects/21-hri-teleoperation/21.04-speed-and-separation-monitoring/)** —
  complete: render/classify kernel (constant-memory capsules, self-filter), min-distance kernel
  (shared-memory tree reduction — exploiting MIN's exact IEEE associativity, a documented
  contrast to SUM), dense field kernel, SSM state machine, CPU twin + double-precision analytic
  oracle (Ericson segment-segment), four gates, distance-field PGM + timeline CSV artifacts,
  full README / THEORY / PRACTICE.
- **[docs/STATUS.md](../docs/STATUS.md)** — 21.04 → `done` (**25/505**).

## New projects (didactic blurbs)

**21.04 — Speed-and-separation monitoring** (★ beginner, domain 21, flagship). The physics of the
protective-distance formula term by term (human approach speed × reaction time, robot speed,
stopping distance, uncertainties), why escalation must be instant while de-escalation waits, and
what a depth pixel can and cannot see — the build surfaced a resolution-*independent* geometric
bias (a vertical capsule's side is invisible to an overhead camera) that was fixed by honest
scenario redesign plus a derived silhouette-sag bound term, all documented as a case study. The
single most interesting thing to look at: `demo/out/ssm_timeline.csv` plotted — d_min crossing
S_p exactly where the state machine reacts.

## How to build & run

```powershell
projects\21-hri-teleoperation\21.04-speed-and-separation-monitoring\demo\run_demo.ps1
# then plot demo\out\ssm_timeline.csv and open demo\out\distance_field.pgm
```

## What to study here

`THEORY.md` §The problem (the S_p formula's physics) and the availability-vs-safety asymmetry
argument → the silhouette-visibility case study → `src/kernels.cu` (the MIN-reduction and why
it is exactly associative where SUM is not). First exercise: halve the camera resolution and
watch the pixel-quantization bound term grow while the silhouette term does not.

## Verification

On the owner's machine (NVIDIA GeForce RTX 2080 SUPER, sm_75, driver 591.86, CUDA 13.3, VS 2026
v145, 2026-07-10), re-run independently by the lead after the builder's self-gate:

- `Release|x64` **and** `Debug|x64` build with **zero errors, zero warnings**.
- `demo/run_demo.ps1` passes end to end: all 11 stable lines matched (including the NOTICE line),
  exit 0.
- **GPU-vs-CPU gate:** pixel labels exact (0/80,000); depth/d_min/dense-field ≤ 1.6e-06 m.
- **Analytic gates:** transitions at frames 74/82/163/171 with **0-frame offset** from the
  closed-form S_p crossings (tol ±1); no-false-stop 0 violations over 139 qualifying frames;
  no-missed-stop 0 violations over 65; d_min within the derived quantization+silhouette bound on
  all 240 frames (worst 0.029 m vs 0.070 m bound).
- Timing (teaching artifact): ≈ 0.11 ms/frame GPU for render + min-distance.
- `tools/verify_project.py`: **all structural gates PASS**; no changes outside the project folder.

## Known limitations / TODOs

- Orthographic top-down depth (documented simplification), illustrative SSM parameters, advisory
  architecture only — the certified layer is explicitly somebody else (the point of PRACTICE §4).

## Next push preview

24.01 2-D magnetostatic FEA + motor torque-ripple sweeps, then 25.01 battery electro-thermal and
26.01 topology optimization close batch 1f.
