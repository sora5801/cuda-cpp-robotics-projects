# expected_output.txt — the stable output contract for project 15.01.
# Lines starting with '#' are comments; run_demo.ps1 / run_demo.sh ignore them when diffing.
# Every non-comment line below must appear VERBATIM in the demo's stdout (subset match). Only
# STABLE lines belong here — "[info]" (GPU name, measured residuals) and "[time]" lines vary by
# machine and are deliberately unchecked; the CONSTRAINTS/VERIFY tolerances carry wide margins
# (documented in src/main.cu and THEORY.md) so the verdict is stable across GPU architectures even
# though the measured residuals themselves are not bit-identical. Change the program's stable lines
# and this file together.
[demo] Minimum-snap trajectory optimization batched over waypoint sets (project 15.01)
PROBLEM: batched minimum-snap trajectories, K=10000 waypoint sets, 4 segments x 8 coeffs/segment x 2 axes = 64 unknowns/set, 5 waypoints/set, dt_seg=1.0 s (4.0 s total), box +/-4.0 m, min spacing 0.75 m, FP32
SAMPLE: loaded 5 hand-designed waypoint set(s) from data/sample/waypoint_sets.csv, incl. 'slalom' [synthetic]
VERIFY: PASS (GPU coefficients match CPU reference within rel tol 5e-03)
CONSTRAINTS: PASS (interpolation/endpoint/continuity residuals and snap-cost sanity within documented tolerances for all 10000 sets)
ARTIFACT: wrote demo/out/trajectory.csv and demo/out/slalom_path.pgm (201 samples)
RESULT: PASS (batch verified against CPU reference and against the minimum-snap constraint definition; artifact written)
