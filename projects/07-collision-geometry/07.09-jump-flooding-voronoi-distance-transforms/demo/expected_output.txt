# expected_output.txt — the stable output contract for project 07.09.
# Lines starting with '#' are comments; run_demo.ps1 / run_demo.sh ignore them when diffing.
# Every non-comment line below must appear VERBATIM in the demo's stdout (subset match). Only
# STABLE lines belong here — "[info]" (deterministic mismatch counts, but machine-formatted paths)
# and "[time]" lines are deliberately unchecked. The output contract is documented at the top of
# ../src/main.cu; change the program's stable lines and this file together, in the same commit.
[demo] jump-flooding Voronoi + distance transform (project 07.09)
PROBLEM: nearest-seed labels + distance field on a 2D grid; JFA (approx, GPU) vs exact scan (CPU)
SAMPLE: 512x512 grid, 64 seeds [synthetic, seed 42]
SAMPLE RESULT: PASS (label mismatches <= 0.5% of cells; max |d_jfa - d_exact| <= 2 cells)
ARTIFACT: wrote demo/out/voronoi.pgm and demo/out/distance.pgm (512x512)
BATCH: 1024x1024 grid, 128 seeds, generated in-memory [seed 42]
BATCH RESULT: PASS (label mismatches <= 0.5% of cells; max |d_jfa - d_exact| <= 2 cells)
RESULT: PASS (JFA within documented approximation bounds of the exact CPU field on all stages)
